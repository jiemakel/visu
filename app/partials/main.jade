.ui.page.grid
  .column
    h1.ui.header.center.aligned (Visual) SPARQL query tool
    #shareicondiv
        i#shareicon.icon(ng-click="shareLink()",ng-class="shareLinkLoading ? 'loading' : 'share'")
    div.ui.form
        div.field(ng-class="{ 'error': !sparqlEndpointInputValid }")
            label SPARQL endpoint
            input(ng-model="sparqlEndpoint",type="text")
        div(id="yasqe")
    div(id="yasr")
